---
import styles from "./circle.module.scss";

interface Props {
  width?: string;
  height?: string;
}

const { width = "44px", height = "44px" } = Astro.props;
---

<div class={styles.circle} style={{ width, height }}>
  <slot />
</div>
